import { getAxios } from "../../helpers/getAxios"

const { data } = await getAxios();
const dataUser = data.data;

export const Post = () => {
    return (
        <div className="post">
            
        </div>
    
  )
}
